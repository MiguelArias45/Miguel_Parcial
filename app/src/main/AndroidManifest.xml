<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Parcial"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiguelParcialApp"
        tools:targetApi="31">

        <!-- Declarar MainActivity como la actividad principal -->
        <activity
            android:name="com.example.miguelparcialapp.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- No declarar AgregarViaje aquí, ya que es un Fragment -->

        <!-- Declarar DetalleViajeFragment si lo usas como una actividad (opcional) -->
        <activity
            android:name="com.example.miguelparcialapp.ui.DetalleViajeFragment"
            android:exported="false" /> <!-- Cambiar a true si es necesario -->

    </application>

</manifest>
